<Window x:Class="NCHops.UmfahrenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Umfahren" Width="360" SizeToContent="Height" MinWidth="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="A +/-:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtA" Text="0" VerticalAlignment="Center"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Startpunkt:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CbStartSide" SelectedIndex="0" VerticalAlignment="Center">
            <ComboBoxItem Content="oben"/>
            <ComboBoxItem Content="rechts"/>
            <ComboBoxItem Content="unten"/>
            <ComboBoxItem Content="links"/>
        </ComboBox>

        <Label Grid.Row="2" Grid.Column="0" Content="Fräsrichtung:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="CbDirection" SelectedIndex="0" VerticalAlignment="Center">
            <ComboBoxItem Content="gegenlauf"/>
            <ComboBoxItem Content="gleichlauf"/>
        </ComboBox>

        <Label Grid.Row="3" Grid.Column="0" Content="Fertigradius r:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtRadius" Text="0" VerticalAlignment="Center"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Z:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtZ" VerticalAlignment="Center"/>

        <Label Grid.Row="5" Grid.Column="0" Content="Werkzeug D:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TxtDiameter" Text="10" VerticalAlignment="Center"/>

        <Label Grid.Row="6" Grid.Column="0" Content="Drehzahl:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="6" Grid.Column="1" x:Name="TxtDrehzahl" Text="18000" VerticalAlignment="Center"/>

        
        <StackPanel Grid.Row="9" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Width="75" Margin="0,0,8,0" Click="OnOk"/>
            <Button Content="Abbrechen" Width="85" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
